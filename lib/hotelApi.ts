/**
 * Mock API (Tuần 5): lưu trữ async + localStorage.
 * UI nên ưu tiên HotelContext và các custom hooks (`useRooms`, `useBookings`, …).
 */
export { HOTEL_STORAGE_KEY, HOTEL_STORE_CHANGED_EVENT, mockApi } from "@/lib/mockApi";
